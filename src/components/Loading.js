import React from "react";
import { CircularProgress } from "@mui/material";

const Loading = () => {
  return (
    <div className=" m-5  ">
      <CircularProgress />
    </div>
  );
};

export default Loading;
